# Optional: enables Gemini responses in the website chatbot.
# Vite exposes VITE_* values to the browser, so use a restricted key and never
# place a private or unrestricted production credential here.
VITE_GEMINI_API_KEY=your_restricted_key
