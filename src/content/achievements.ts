import type { Achievement } from './types';

/**
 * Verified achievement records. Empty by design in Phase 2 — no achievement records have been
 * supplied yet (categories will include accepted research papers, publications, international
 * participations, awards, STEM milestones, project outcomes, community impact). No demo or
 * placeholder achievements are invented. See `contentGaps.ts`.
 */
export const ACHIEVEMENTS: Achievement[] = [];
