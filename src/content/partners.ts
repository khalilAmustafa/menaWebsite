import type { Partner } from './types';

/**
 * Verified partners & sponsors for the new content layer. Empty by design in Phase 2 — no
 * official partner/sponsor list, classification, logos, or URLs have been supplied.
 *
 * NOTE: the homepage's decorative partner icons (component-local `PARTNERS` in
 * `src/components/Partners.tsx`) are NOT authoritative and are intentionally not migrated here.
 * See `contentGaps.ts`.
 */
export const PARTNERS: Partner[] = [];
